# Tests
pytest>=7.0.0
pytest-cov>=4.0.0

# Linting
pylint>=4.0.0
black==26.5.1

# Type checking
mypy>=2.0.0

# Runtime libs that on the Pi come from apt (python3-opencv) but on a local
# dev machine have no apt equivalent, so they must be pip-installed to run the
# test suite / the ml pipeline off-Pi. Kept out of requirements.detector.txt so
# the ARM Docker build never pip-installs opencv (see the note there).
# 4.7.0.72 is a real published wheel (the old bare "4.7.0" pin resolved to
# nothing) and is compatible with the numpy 1.24.x pinned in the detector reqs.
opencv-python-headless==4.7.0.72

# Progress bar for the dev-only tools/build_species_reference.py builder (shows
# per-species rate/ETA over the long reference-bank build). Dev-only; never
# installed on the Pi / in the Docker images.
tqdm>=4.66

# Used only by tools/camera_smoke_e2e.py (dev-only Pi SSH smoke test); never
# installed on the Pi / in the Docker images. matplotlib >=3.7 needs only
# numpy>=1.20, so it stays compatible with the numpy 1.24.2 pin above and does
# not drag numpy 2.x in.
matplotlib>=3.7
paramiko>=3.0

# Off-Pi camera emulator (dev/emulation + dev/run_emulated):
# runs the detector locally against a real YOLO11n object detector via the
# already-pinned onnxruntime — NO runtime ultralytics dependency. It needs a
# `assets/models/yolo11n.onnx` export, which is out-of-band (git-ignored, like
# the classifier ONNX). Produce it once on a dev machine with ultralytics:
#     pip install ultralytics && yolo export model=yolo11n.pt format=onnx
# then move yolo11n.onnx into assets/models/. The emulator + its tests skip
# gracefully when the file is absent.