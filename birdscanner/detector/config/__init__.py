"""Static defaults plus the user-editable runtime settings overlay.

The static :class:`Config` defaults (:mod:`~birdscanner.detector.config.config`), the
JSON settings overlay layered on top of them (:mod:`~birdscanner.detector.config.settings`),
and the controller that applies settings to the running detector
(:mod:`~birdscanner.detector.config.settings_controller`).
"""
