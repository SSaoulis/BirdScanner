"""Pi-only camera + hardware control and the application entry point.

Modules here depend on :mod:`picamera2` / :mod:`libcamera`, which are only available on
the Raspberry Pi. Run the detector with ``python -m birdscanner.detector.main``.
"""
