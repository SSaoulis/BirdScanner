**/__pycache__
**/*.pyc
**/*.pyo
.git
.gitignore
.env
*.md
tests/
frontend/node_modules/
frontend/.vite/
camera_view/
camera_view.tar
tools/
.claude/
.agents/
# The detector/api Dockerfiles COPY only the specific files they need out of
# assets/models/ (the int8 ONNX classifier + its class map), so any training/
# conversion scratch living alongside them is never added to an image.
*.egg-info/
dist/
