"""Developer / offline tooling — not part of the BirdScanner runtime.

These scripts run on a dev machine or the Pi console, never inside the detector/api
services (they are excluded from the Docker images). Examples: the species-reference
data-bank builder and camera smoke tests.
"""
